import * as React from 'react'

import { Container, Typography } from '@mui/material'

export const HomePage: React.FC = () => (
  <Container sx={{ my: 2 }}>
    <Typography variant="h5" sx={{ mb: 1 }}>Home Page</Typography>
  </Container>
)
